# 创建密钥

API 密钥用于客户端鉴权。建议为不同客户端分别创建密钥，以便后续排查和撤销。

## 创建步骤

1. 登录 SurplusToken 平台。
2. 进入密钥管理页面。
3. 选择创建密钥。
4. 输入便于识别的名称，例如 `个人电脑 Codex`。
5. 创建后立即保存完整密钥。

:::warning
完整密钥通常只会显示一次。丢失后请创建新密钥，并撤销旧密钥。
:::

## 管理建议

- 不要把密钥直接写入代码或提交到 Git。
- 优先使用环境变量或客户端提供的安全凭证存储。
- 不再使用某台设备或客户端时，及时撤销对应密钥。
